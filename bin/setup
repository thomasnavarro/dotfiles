# #!/bin/bash

# This script is currently disabled.
# It can be adapted to quickly setup any type of project.
# Customize it according to your specific project setup workflow.
#
# Usage: Uncomment and customize the lines below for your own project setup workflow

# echo "Setting up project..."

# REPOSITORY=$1
# DIRECTORY="./$2"

# if [ -z "$REPOSITORY" ] || [ -z "$DIRECTORY" ]; then
#     echo "Please provide both a repository and target directory."

#     exit 1
# fi

# git clone $REPOSITORY $DIRECTORY
# cd $DIRECTORY

# # Add your project setup steps here
# # Examples:
# # npm install
# # composer install
# # cp .env.example .env

# open -a "/Applications/Visual Studio Code.app" "`pwd`"
